{
  "name": "cf_ai_llama33_edgechat",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "pages:dev": "wrangler pages dev app --ai",
    "pages:deploy": "wrangler pages deploy app",
    "typecheck": "tsc --noEmit",
    "build": "echo Workers bundle is handled by Wrangler"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251002.0",
    "typescript": "^5.6.3",
    "wrangler": "^3.80.0"
  }
}